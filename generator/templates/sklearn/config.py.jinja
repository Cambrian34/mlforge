"""config.py — Generated by MLForge v{{ mlforge_version }}"""
# Project: {{ project_name }}
# Edit these values to match your dataset and environment.

# ── Dataset ───────────────────────────────────────────
DATASET_PATH   = "{{ dataset.file_path or 'data/dataset.csv' }}"
TARGET_COLUMN  = "{{ dataset.target_column or 'target' }}"

# ── Train / Test split ────────────────────────────────
TEST_SIZE      = {{ training.test_size }}
RANDOM_STATE   = {{ training.random_state }}

# ── Model checkpoint ──────────────────────────────────
CHECKPOINT_PATH = "checkpoints/model.joblib"
