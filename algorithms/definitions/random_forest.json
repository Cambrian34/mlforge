{
  "id": "random_forest",
  "name": "Random Forest",
  "category": "ensemble",
  "framework": "sklearn",
  "tasks": ["classification", "regression"],
  "description": "Ensemble of decision trees using bagging and feature randomness.",
  "sklearn_class_classification": "sklearn.ensemble.RandomForestClassifier",
  "sklearn_class_regression": "sklearn.ensemble.RandomForestRegressor",
  "parameters": {
    "n_estimators": {
      "type": "integer",
      "default": 100,
      "min": 1,
      "max": 2000,
      "label": "Number of Trees",
      "description": "The number of trees in the forest."
    },
    "max_depth": {
      "type": "integer",
      "default": null,
      "min": 1,
      "max": 100,
      "nullable": true,
      "label": "Max Depth",
      "description": "Maximum depth of each tree. None = unlimited."
    },
    "min_samples_split": {
      "type": "integer",
      "default": 2,
      "min": 2,
      "max": 100,
      "label": "Min Samples Split",
      "description": "Minimum samples required to split an internal node."
    },
    "min_samples_leaf": {
      "type": "integer",
      "default": 1,
      "min": 1,
      "max": 100,
      "label": "Min Samples Leaf",
      "description": "Minimum samples required at a leaf node."
    },
    "criterion_classification": {
      "type": "enum",
      "values": ["gini", "entropy", "log_loss"],
      "default": "gini",
      "label": "Criterion (Classification)",
      "description": "Function to measure split quality (classification only).",
      "task_filter": "classification"
    },
    "criterion_regression": {
      "type": "enum",
      "values": ["squared_error", "absolute_error", "friedman_mse"],
      "default": "squared_error",
      "label": "Criterion (Regression)",
      "description": "Function to measure split quality (regression only).",
      "task_filter": "regression"
    },
    "random_state": {
      "type": "integer",
      "default": 42,
      "min": 0,
      "label": "Random State",
      "description": "Seed for reproducibility."
    }
  },
  "template": "sklearn/model.py.jinja"
}
